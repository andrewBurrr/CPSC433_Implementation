package Structures;

public class Course {

    private final String identifier;

    public Course(String identifier) {
        this.identifier = identifier;
    }

    public String getIdentifier() { return identifier; }

    // section
    // labs list children
    // create classes
    // otree - node
    // fact - ((a1,b1),...,(an,bn)) complete solution not null
    // pr (problem) - ((a1,b1),...,(an,bn)) can be either partial or complete
    // Sset subset of Fpow

}
